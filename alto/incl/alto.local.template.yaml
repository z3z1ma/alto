# this file should be gitignored
# it is merged into the main config file at runtime
# and follows the exact same structure,
# use it to include personal settings

default:
  environment: {}
  taps: {}
  targets: {}
  utilities: {}
